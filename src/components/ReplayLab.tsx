import { replayCases } from '../data/snapshots'
import { useState } from 'react'

export function ReplayLab() {
  const [checked, setChecked] = useState(false)
  return <main className="content-view replay-view"><div className="view-heading"><div><h1>Replay Lab</h1><p>Point-in-time protocol. Models may see only evidence available at the frozen timestamp.</p></div><button className="primary-button" onClick={() => setChecked(true)}>{checked ? 'Protocol disclosed' : 'Inspect protocol'}</button></div><section className={`panel benchmark-summary ${checked ? 'checked' : ''}`}><div><strong>4</strong><span>demonstration cases</span></div><div><strong>0</strong><span>cases claimed as historical</span></div><div><strong>5</strong><span>planned evaluation metrics</span></div><div><strong>12–20</strong><span>target frozen cases</span></div></section><section className="panel replay-table"><table><thead><tr><th>Case</th><th>Instrument</th><th>Frozen scenario</th><th>Expected label</th><th>Current status</th></tr></thead><tbody>{replayCases.map((item) => <tr key={item.id}><td>{item.id}</td><td>{item.symbol}</td><td>{item.scenario}</td><td>{item.expected}</td><td className="caution-text">DEMO FIXTURE</td></tr>)}</tbody></table><p className="table-note">No accuracy metric is claimed from these fixtures. Historical results will be published only with immutable evidence bundles, time cutoffs, labels, and evaluator code.</p>{checked ? <p className="protocol-note">Planned comparison: rules-only vs. LLM-only vs. hybrid on state accuracy, unsupported-claim rate, temporal-contradiction catch rate, abstention precision, and median task time.</p> : null}</section></main>
}
